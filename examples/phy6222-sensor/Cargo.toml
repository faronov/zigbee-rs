[package]
name = "phy6222-sensor"
version = "0.1.0"
edition = "2024"

[dependencies]
sensor-sed-app = { path = "../../apps/sensor-sed" }
zigbee-mac = { path = "../../zigbee-mac", default-features = false, features = ["phy6222"] }
zigbee-runtime = { path = "../../zigbee-runtime", default-features = false, features = ["constrained-memory"] }
phy62x2-evk = { path = "../../boards/phy62x2-evk", default-features = false }
phy62x2-evk-product = { path = "../../products/phy62x2-evk", default-features = false }

# Embassy async runtime
embassy-executor = { version = "0.7", default-features = false, features = ["arch-cortex-m", "executor-thread"] }
embassy-time = { version = "0.5", default-features = false }

# ARM Cortex-M0 support
cortex-m = { version = "0.7", features = ["critical-section-single-core"] }
cortex-m-rt = { version = "0.7", features = ["device"] }

# Compile out transitive logging in the production image.
log = { version = "0.4", default-features = false }

# Panic handler for embedded target
panic-halt = "1.0"

[features]
default = ["production", "phy6222"]
production = ["log/release_max_level_off"]
phy6222 = ["phy62x2-evk/phy6222", "phy62x2-evk-product/phy6222"]
phy6252 = ["phy62x2-evk/phy6252", "phy62x2-evk-product/phy6252"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
